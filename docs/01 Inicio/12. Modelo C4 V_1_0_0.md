# Arquitectura de Software – Modelo C4

| Campo | Detalle |
| --- | --- |
| **Proyecto** | EcoLogística Lima – Optimizador de Rutas Sostenibles |
| **Integrantes** | Josue [Apellidos] · [Nombre integrante 2] · [Nombre integrante 3] |
| **Fecha** | 25 de agosto de 2026 |
| **Versión** | 1.0.0 |

---

## Nivel 1: Diagrama de Contexto

```mermaid
graph TD
    Despachador[Despachador / Operador] -->|Utiliza vía HTTPS| Sistema[Sistema EcoLogística Lima]
    Conductor[Conductor - App móvil] -->|Utiliza vía HTTPS| Sistema
    Administrador[Administrador] -->|Utiliza vía HTTPS| Sistema
    Auditor[Auditor Externo] -->|Consulta vía HTTPS - solo lectura| Sistema
    Sistema -->|Consulta tráfico y geocodificación| ExtMapas[Servicio Externo de Mapas y Tráfico - OSM / Waze CCP]
    Sistema -->|Autentica sesión| ExtAuth[Proveedor de Identidad / OAuth2]
    Sistema -->|Envía notificaciones| ExtNotif[Servicio de Notificaciones - Email / SMS]
```

## Nivel 2: Diagrama de Contenedores

```mermaid
graph TD
    subgraph Sistema EcoLogística Lima
        SPA[Aplicación Web SPA - React] -->|REST / JSON sobre HTTPS| API[API Backend - FastAPI]
        AppMovil[App Conductor - Web responsiva offline-first] -->|REST / JSON sobre HTTPS| API
        API -->|Lectura / Escritura SQL| DB[(PostgreSQL + PostGIS)]
        API -->|Caché de sesión y colas| Cache[(Redis)]
        API -->|Invoca| Motor[Servicio Motor de Optimización - VRPTW / Green VRP]
        Motor -->|Lee / Escribe resultados| DB
    end
    API -->|Consulta tráfico| ExtMapas[API externa de Mapas y Tráfico]
    API -->|Valida credenciales| ExtAuth[Proveedor OAuth2]
    API -->|Envía eventos| ExtNotif[Servicio de Notificaciones]
```

## Nivel 3: Diagrama de Componentes (Contenedor: API Backend)

```mermaid
graph TD
    subgraph API Backend - FastAPI
        MW[Middleware de Seguridad - Auth / RBAC] --> CtrlUsuarios[Controlador Usuarios y Roles]
        MW --> CtrlFlota[Controlador Flota y Conductores]
        MW --> CtrlPedidos[Controlador Pedidos]
        MW --> CtrlRutas[Controlador Rutas e Incidentes]
        MW --> CtrlReportes[Controlador Dashboard y Reportes]

        CtrlUsuarios --> SrvUsuarios[Servicio de Usuarios]
        CtrlFlota --> SrvFlota[Servicio de Flota]
        CtrlPedidos --> SrvPedidos[Servicio de Pedidos]
        CtrlRutas --> SrvRutas[Servicio de Orquestación de Rutas]
        CtrlReportes --> SrvReportes[Servicio de Reportes y KPIs]

        SrvRutas --> ClienteMotor[Cliente del Servicio Motor de Optimización]
        SrvRutas --> RepoRutas[Repositorio de Rutas]
        SrvUsuarios --> RepoUsuarios[Repositorio de Usuarios]
        SrvFlota --> RepoFlota[Repositorio de Flota]
        SrvPedidos --> RepoPedidos[Repositorio de Pedidos]
        SrvReportes --> RepoReportes[Repositorio de Reportes]

        RepoRutas --> DB[(PostgreSQL + PostGIS)]
        RepoUsuarios --> DB
        RepoFlota --> DB
        RepoPedidos --> DB
        RepoReportes --> DB
    end
```

**Descripción de componentes internos de la API:**

- **Middleware de Seguridad:** valida el token de sesión, resuelve el rol del usuario y aplica las reglas de control de acceso (RBAC) definidas en el documento 08 antes de despachar la petición a cualquier controlador.
- **Controladores:** exponen los endpoints REST agrupados por dominio funcional (usuarios, flota, pedidos, rutas, reportes), y delegan la lógica de negocio a los servicios correspondientes.
- **Servicios:** contienen la lógica de negocio y las reglas descritas en el documento 09 (por ejemplo, `SrvRutas` aplica RN-002, RN-003 y RN-004 antes de invocar al motor de optimización).
- **Cliente del Servicio Motor de Optimización:** capa de integración desacoplada que permite sustituir la metaheurística (Algoritmo Genético, Búsqueda Tabú, ACO) sin modificar los controladores ni los repositorios, conforme al requisito de mantenibilidad RNF-010.
- **Repositorios:** encapsulan el acceso a datos hacia PostgreSQL/PostGIS, aislando el resto de la aplicación de detalles de persistencia.
